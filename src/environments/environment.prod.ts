// to deploy and test, install a HTTP server. Run $ npm i -g http-server;
// do build, run $ ng build in the root path;
// So start the project, inside dist/ngen-charts-project, run $ http-server.

export const environment = {
    production: true
};
